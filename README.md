# 🏥 도수치료예약 (Dosu Clinic)

> 도수치료 전문 병원을 위한 **환자·예약·직원·통계** 통합 관리 시스템
> Windows 단독 실행형 · 로컬 SQLite · 멀티-PC 동기화 지원

이 리포는 **배포·자동 업데이트 전용** 입니다. 실제 소스 코드는 별도로 관리되며, 이곳에는 완성된 ZIP 과 업데이트 매니페스트만 올라갑니다.

---

## 📋 주요 기능

### 예약 관리
- 일자별 **치료사 × 시간대** 보드 (드래그로 시간·담당자 변경)
- 체외충격파·주사·연골주사 공용 예약 · 도수치료 개인 담당 예약
- 금일 예약 환자 / 취소 목록 / 미니 달력 사이드바
- 원무과 승인(완료 처리) + 낙관적 락으로 다중 PC 동시 수정 안전
- **기간별 집계 & 통계** (v1.2.9+) — 시작일~종료일 + 프리셋 (오늘/7일/이번 달/지난 달/30일)

### 환자 관리
- 8만 명+ 대용량 대응 (서버 검색 + 페이지네이션)
- 엑셀 **데이터변환** — 기존 환자 명단 일괄 불러오기
- 성별·차트번호·연락처·생년월일 자동 인식
- 치료항목별 처방/완료 횟수 자동 카운트

### 문자 발송 (문자나라 연동)
- 예약 안내 · 리마인더 템플릿
- 변수 치환 (`{환자명}`, `{다음예약날짜}` 등)
- 발송 로그 DB 저장
- 한글 인코딩 자동 (v1.2.4+)

### 통계 & 집계
- 기간 요약 5카드 (예약/완료/도수/취소)
- **매출 중심 분석** — 치료항목별·치료사별 예상 매출
- 치료사별 기간 집계 + **인센티브 자동 계산** (% 또는 고정금액)
- **체외충격파 수동 입력** (v1.2.7+) — 집계 탭에서 더블클릭으로 당일 환자 수 직접 입력

### 치료항목 동적 관리
- 도수 30/60/90분 등 시간 항목 자유 추가
- 수가(가격) · 치료사 인센티브 설정
- 활성/비활성 전환 · 표시 여부 제어

### 시스템
- 관리자 비밀번호 보호 · 감사 로그
- 자동 백업 (기본 60분 간격 · 30개 보관)
- 수동 백업/복원 버튼
- DB 무결성 점검 도구
- **네이비 헤더 디자인** (v1.2.5+) · 상단 접속 주소 복사 (v1.2.6+)

---

## 🔄 자동 업데이트

v1.2.4 부터 **버튼 3클릭으로 최신 버전으로 자동 업그레이드** 됩니다.

### 최초 1회 설정
1. 프로그램 실행 → **관리자 → ≡ 시스템**
2. **"업데이트 매니페스트 URL"** 칸에 입력:
   ```
   https://hu28035036-ux.github.io/clinic-updates/manifest.json
   ```
3. **[URL 저장]** 클릭

### 업데이트 시
1. **[업데이트 확인]** → 새 버전 있으면 변경사항 미리보기
2. **[⬇ 업데이트 다운로드]** → ZIP 자동 다운로드 + SHA256 검증
3. **[✔ 지금 설치]** → 설치 직전 **DB 자동 백업** + 15~20초 재시작

### 안전 장치
- ✅ 설치 직전 **DB 자동 스냅샷** (`clinic_before_update_*.db`)
- ✅ **SHA256 검증** 으로 파일 무결성 확인
- ✅ 실패 시 **자동 롤백** (이전 버전으로 복구)
- ✅ 데이터 폴더(`%APPDATA%\도수치료예약\`)는 업데이트 영향 없음
- ✅ **CSS 캐시 자동 무효화** (v1.2.8+) — 디자인 변경이 업데이트 즉시 반영

---

## 💻 시스템 요구사항

| 항목 | 권장 |
|---|---|
| OS | Windows 10 / 11 (64-bit) |
| RAM | 4GB 이상 |
| 디스크 | 200MB (프로그램) + DB 크기 |
| 인터넷 | 자동 업데이트 시만 필요 · 평상시 오프라인 동작 |
| 브라우저 | Chrome / Edge / Whale 최신 (프로그램이 로컬 웹서버로 동작) |

**완전 오프라인 환경에서도 정상 작동** — 문자 발송, 자동 업데이트 외의 모든 기능이 로컬에서 동작합니다.

---

## 📂 데이터 저장 위치

```
C:\Users\<사용자>\AppData\Roaming\도수치료예약\
├── clinic.db               환자·예약·직원·치료항목 DB (SQLite)
├── config.json             관리자 설정 · 매니페스트 URL
├── schema_version.txt      마이그레이션 버전 기록
└── backups\
    ├── clinic_20260420_120000.db              (자동 백업)
    ├── clinic_before_update_v1.2.x_*.db       (업데이트 직전 자동)
    └── ...
```

⚠️ **프로그램 폴더 업데이트 시 데이터는 전혀 영향받지 않습니다.**

---

## 📦 설치 방법

### 신규 설치
1. [Releases](https://github.com/hu28035036-ux/clinic-updates/releases) 에서 최신 ZIP 다운로드
2. 원하는 경로에 압축 해제 (예: `C:\Program\도수치료예약\`)
3. `도수치료예약.exe` 더블클릭
4. 바탕화면 바로가기 자동 생성됨 · 브라우저에서 자동 열림

### 기존 설치에서 업그레이드 (수동)
자동 업데이트 사용 못 하는 경우:
1. 프로그램 종료
2. 최신 ZIP 압축 해제 후 **기존 폴더 덮어쓰기**
3. 재실행

---

## 📜 버전 히스토리

### v1.3.1 (2026-04-30) · 🆕 최신
- 🛡️ **자동 업데이트 hang 수정** — "지금 설치" 후 화면이 "업데이트 중" 에서 멈추던 사고 해결
- 🔧 updater.bat: `_internal`/`exe` rename 5회 재시도 (안티바이러스/Windows 인덱싱 잠금 해제 대기), 본체 종료 후 3초 대기로 .pyd/.dll unlock 시간 확보
- 🔧 PowerShell `Expand-Archive` 에 `-ExecutionPolicy Bypass` 명시 (병원 GPO 환경 대응), rollback 콘솔은 `pause` 로 사용자가 메시지 끝까지 확인 가능
- 🔧 apply_update creationflags `DETACHED_PROCESS|CREATE_NEW_PROCESS_GROUP|CREATE_BREAKAWAY_FROM_JOB` (부모 종료 시 자식 잡힘 방지), 자식 콘솔 `/k → /c` (정상 종료 후 자동 닫힘)
- 🆕 진단: `GET /api/about/update-log` + 프론트 30초/60초/6분 단계별 안내 박스 + 새 서버 health-check 자동 새로고침
- 🛡️ **인증 누수 회귀 수정** — `/api/sync/pull/push/now`, `/api/backup`, `/api/mode` 인증 강화 (이전: 같은 네트워크 누구나 DB 파일/sync 큐 노출 가능)
- 🛡️ **sync 듀얼 인증** — 관리자 토큰 OR `X-Sync-Token` (config.sync_secret 자동 생성). peer 노드 페어링용 `/api/config/sync-secret` (관리자 전용) 신규
- 🛡️ **GET /api/config 에서 sync_secret 자동 제거** (이전 회귀: 익명 노출 가능했음)
- 🛡️ **DB 복원 안전성** — 임시 파일 + `PRAGMA integrity_check` + atomic 교체 + `engine.dispose()` (Windows 파일 잠금 회피)
- 🛡️ **SMS 비밀 노출 차단** — 서버 echo 응답 / urllib 예외 메시지에 평문 passwd/key 가 stderr / DB 에 남던 사고 방지 (자동 마스킹)
- ⚙️ 데이터 변환(엑셀→환자) 적용 시 spinner + 버튼 비활성화 + 90초 타임아웃, 모달 액션 버튼 sticky
- ✅ 회귀 방지 테스트 +45건 (124 passed)
- 🗄️ DB 변경 없음 (config.json 에 sync_secret 자동 추가)

### v1.3.0 (2026-04-30) · AI/RAG 1차 통합 (v1.3.1 에 합본 적용)
- ✨ **AI 도우미 탭** — 직원이 자연어로 업무 매뉴얼 질문 → 답변 + 출처 + 신뢰도 표시
- 📚 신규 매뉴얼 6종: 예약문자 작성 / 치료사 미배정 시 문자 / 문자나라 오류 대응 / 백업 / 치료사 휴무 / AI 설정
- 🔍 키워드 RAG (벡터DB / 임베딩 미사용 — 1차 단계). 매뉴얼에 없는 질문은 LLM 호출 없이 "매뉴얼에서 답을 찾지 못했습니다."
- ✨ **예약문자 AI 초안 + 발송 전 점검** — `/api/ai/sms/draft`, `/api/ai/sms/validate` (관리자 → AI 설정에서 키 입력 + 활성화 후 사용)
- 🛡️ **PII 보호** — 환자 PII (이름/전화/생년월일/차트/메모) 검색·전달 차단, 사용자 입력 PII 자동 마스킹, LLM 응답 사후 환각 검증
- 📊 마이그레이션 m007 (ai_settings) + m008 (ai_usage_logs 8컬럼 확장)
- 🔒 기본값 enabled=0 — API Key 입력 + 활성화 전까지 모든 LLM 호출 차단

### v1.2.18 (2026-04-28)
- 🐞 **문자나라 발송 결과 오판정 수정** — 문자가 정상 도착했는데도 화면엔 "발송 거부 (code=9)" 알람이 뜨던 버그
- 🔍 원인: vendor 가 운영 환경에서 신식 성공 응답 `9|<메시지ID>|<건수>|<발신자명>` (예: `9|103269|1|이중성`) 을 보내는데, 기존 코드는 첫 필드 `9` 만 보고 거부로 판정
- 🛡️ 수정: 두 번째 필드가 메시지ID 형식(3자리 이상 숫자) 이면 성공으로 인정. `MISSING/ERROR/FAIL/INVALID/EMPTY` 키워드는 여전히 거부 (안전망)
- ✨ **통계탭 엑셀 다운로드** — 조회 기간 보고서를 .xlsx 로 출력 (통계 탭 [📊 엑셀 다운로드] 버튼)
- 📑 시트 3개: **요약** (KPI + 날짜별 + 치료사별 + 재진률·예상매출 비교표) / **일별통계** / **치료사별집계** (합계 행 포함)
- 🎨 A4 가로 / 가로 1페이지 맞춤 / 맑은 고딕 10~11pt(제목 16pt) / 진한 남색 헤더 / 본문 교차 배경 / 얇은 회색 테두리 / 금액 `₩#,##0` / 재진률 `0.0%`
- 🔧 도수치료 항목명 정식명 자동 반영 (새 항목 추가 시 컬럼 자동 생성), **미배정 / 주사 컬럼 / "순위" 단어 모두 미사용**
- 🐞 통계탭 엑셀 예상매출에 **체외충격파 수동 입력분 포함** — 통계탭 화면과 엑셀의 치료사별 매출 숫자 일치
- 🗄️ DB 변경 없음 — 자동 업데이트로 데이터 그대로 유지

### v1.2.17 (2026-04-28)
- ✨ **점심시간 시스템 설정** — 관리자 ≡ 시스템 운영시간 카드에 점심 옵션 3개 추가 (사용 여부 / 시작 / 종료, 기본 12:30~13:30)
- 🍱 켜두면 예약 보드의 그 시간대 행이 **"점심시간" 한 셀로 가로 병합** 되고 신규 예약·시간 수정·드래그 이동 모두 차단됨 (프론트 alert + 서버 HTTP 400 이중 검증)
- 🛡️ **기존 예약 보호** — 점심창과 시간이 겹치는 예약이 그 날에 있으면 병합을 자동 풀고 평소처럼 셀 표시 (점심시간 새로 설정해도 과거 예약이 깨지지 않음)
- 🎚️ **기본값 OFF** — 사용자가 직접 켜야 적용 (기존 사용자 영향 없음)
- ✨ **도수치료 예약현황 엑셀 다운로드** — 예약 보드 헤더 `[📊 도수치료 엑셀]` 버튼. **A4 가로 1페이지** 인쇄용 출력 (통계 X, 현장에서 적기/보기 위한 단순 표)
- 📊 도수 가능 활성 치료사 + "미배정" 1열 / 운영시간 30분 단위 / 도수예약이 늦게 끝나면 자동 확장 / 60분은 2칸 세로 병합 / 충·주 suffix 표시
- ✨ **신규 환자 중복 등록 차단** — 차트번호 중복 또는 이름+생년월일 중복 시 409 차단. 이름만 같음 / 전화만 같음 → 차단 안 함 (동명이인 허용)
- 🗄️ **DB 변경 없음** — 자동 업데이트로 데이터 그대로 유지

### v1.2.16 (2026-04-25)
- 🐞 **문자나라 로그인 정보 초기화 버그 수정** — 설정 저장할 때마다 2차 비밀번호가 빈값으로 덮어씌워지던 버그
- 🔍 원인: `<input type="password">` 가 GET 값을 안 채워서 폼이 빈 input 으로 그려짐 → 다른 필드만 수정해도 빈 비번이 같이 PUT 됨
- 🛡️ 수정: 백엔드 `/api/sms/setting` 에서 password 류 빈 값은 "수정 안 함" 으로 해석 (기존 DB 값 보존)
- 📲 적용 후 한 번 더 2차 비번 입력 + 저장 (이전 빌드에서 빈 값으로 덮어쓰기 됐을 수 있음)

### v1.2.15 (2026-04-25)
- 🐞 **문자나라 SMS 한글 깨짐 진짜 수정** — UTF-8 → CP949 인코딩 전환
- 🔍 원인: 문자나라 서버가 `charset=utf-8` 헤더를 무시하고 자체적으로 EUC-KR/CP949 로 해석 → 한글이 `{? 샟역紐` 처럼 깨져 도착
- ✨ `urllib.parse.urlencode(params, encoding='cp949', errors='replace')` + `Content-Type: charset=euc-kr` 로 변경
- 🛡️ 이모지 등 CP949 미지원 글자는 `?` 로 안전 치환 → 발송 자체는 막히지 않음
- ⚠️ `code=9` 발송 거부는 별개 (계정/포인트/발신번호 이슈 — 문자나라 사이트에서 처리)

### v1.2.10 ~ v1.2.14 (2026-04-24 ~ 25) — 예약 표 반응형 레이아웃
- 📐 **예약 탭 메인 표가 창 크기에 맞춰 가로·세로 자동 확장** · 와이드 모니터에서 화면 끝까지 꽉 참
- 🎯 진짜 원인: `.tab-pane { max-width: 1500px }` 가 상위에서 잘라내고 있었음 (v1.2.14 에서 발견 · 예약 탭만 제한 해제)
- 📐 **가로**: 치료사 열 최소 150px 보장 → 많으면 표 자체가 옆으로 늘어나 가로 스크롤 · 적으면 열이 확장
- 📏 **세로**: flex 레이아웃으로 표가 창 높이에 맞춰 늘어남 (최소 행 높이 44px 유지)
- 🖱️ 업데이트 설치 후 `Ctrl + Shift + R` 한 번 (CSS 캐시 갱신)

### v1.2.9 (2026-04-24)
- 📅 **집계·통계 '기간' 조회 도입** — 시작일~종료일 + 프리셋 5종
- 🎛️ 프리셋 버튼: 오늘 / 최근 7일 / 이번 달 / 지난 달 / 최근 30일
- 🔧 API 확장 (`date_from`/`date_to`) + 하위호환 (`year`/`month` 유지)
- ✨ "월합계" → "기간합계" · 요일 색상(토·일) 유지

### v1.2.8 (2026-04-24)
- 🔧 **체외충격파 집계 수동 입력 전용**으로 단일화 (예약 기반 합산 제거)
- 🐞 **CSS 캐시 자동 무효화** (`?v=APP_VERSION`) — v1.2.5~1.2.7 디자인 미반영 이슈 근본 해결
- ✨ `✎N` 뱃지 제거, 표시값 = 입력값 자체
- 📊 집계/통계 책임 분리 (집계=운영용 수동입력, 통계=예약 기반 분석)

### v1.2.7 (2026-04-23)
- ⚡ **체외충격파 수동 입력** 기능 (집계 탭 더블클릭)
- 🗄️ 마이그레이션 006 — `manual_counts` 테이블 추가
- 📊 수동 입력분이 집계·통계 자동 반영
- ✨ 0 입력 시 자동 삭제

### v1.2.6 (2026-04-23)
- 🐞 예약 현황 표에 운영 종료 시각(18:30) 라인 누락 수정
- 🔗 상단 접속 주소 표시 + 서브 주소 복사 버튼
- 🛰️ 서브 IP 자동 감지 개선 (실제 LAN IP 우선)

### v1.2.5 (2026-04-23)
- 🎨 **색상 디자인 리뉴얼** — 네이비 헤더 + 밝은 본문
- 🏷️ 서브 박스 제목줄 블루-슬레이트 톤 계층 분리
- 👁️ 보조 글씨 가독성 강화 · placeholder 선명도 up
- 🔤 카드 제목 넘침 수정 (줄바꿈 정상화)

### v1.2.4 (2026-04-21)
- 🔄 **자동 업데이트 기능 완성** (버튼 3클릭)
- 🛡️ 업데이트 전 **DB 자동 백업** (SQLite backup API)
- 🐞 문자나라 한글 깨짐 수정 (POST 전환 + 인코딩 자동 감지)
- ✨ SHA256 접두사 자동 처리

### v1.2.3 (2026-04-20)
- 💰 **치료항목 수가/인센티브** 기능 신규
- 📊 통계 **"분석"** 개편 (매출 중심 2카드)
- ▥ 집계 탭 **치료사별 인센티브** 행 추가
- 🐞 도수치료 시간항목 집계 버그 수정

### v1.2.2 (2026-04-19)
- 🛡️ **증분 마이그레이션** 시스템 도입 (DB 영구 보존 보장)
- 🔧 **CDN 라이브러리 번들** (오프라인·방화벽 환경 지원)
- 🛠️ `백업하기.bat` · `복원하기.bat` · `db점검.bat` 도구 추가

### v1.2.1 (2026-04-19)
- 🔧 openpyxl 누락 핫픽스
- 📑 CSV fallback 파싱 추가

### v1.2.0 (2026-04-18)
- 환자 성별(M/F) 필드 + 라디오 버튼
- 데이터변환 성별/주민번호 자동 인식

### v1.1.0 (2026-04-18)
- 데이터변환 서브탭 (엑셀 → DB 일괄)
- 문자나라 API 연동
- 예약현황표 드래그 시간+치료사 동시 변경
- 파스텔 병원 테마 UI

---

## 🧪 기술 스택

- **백엔드**: Python 3.12 · FastAPI · SQLAlchemy · SQLite
- **프론트엔드**: Jinja2 템플릿 · Alpine.js · FullCalendar · SortableJS
- **배포**: PyInstaller onedir (단독 실행형 .exe)
- **동기화**: 커스텀 op-log 기반 last-write-wins
- **자동 업데이트**: GitHub Releases + Pages 매니페스트

---

## 🛟 문제 해결

### 프로그램이 안 열려요
1. `도수치료예약.exe` 가 있는 폴더 확인
2. 작업 관리자에서 기존 프로세스 있으면 종료
3. 방화벽/백신 차단 확인 (로컬 포트 8000 사용)

### 디자인이 안 바뀌었어요 (업데이트 후)
- **v1.2.8 이상으로 업데이트** — CSS 캐시가 자동 무효화됨
- 그래도 안되면 브라우저 `Ctrl+Shift+R` 강력 새로고침

### 업데이트 후 이상해요
- 자동 롤백이 동작하면 이전 버전으로 복구됨
- 수동 복구: 프로그램 폴더에서 `_internal.old` → `_internal`, `도수치료예약.exe.old` → `도수치료예약.exe` 로 이름 변경

### 데이터가 사라졌어요
- 실수로 `%APPDATA%\도수치료예약\` 폴더를 건드린 경우 외엔 사라질 수 없음
- `backups\` 안의 최신 `.db` 파일로 **복원하기.bat** 실행

### 문자가 깨져서 도착해요
- v1.2.4 이상으로 업데이트
- 문자나라 발신번호 사전 등록 확인
- 광고성 문자는 `(광고)` + 무료 수신거부 번호 필수

### 서브 PC 에서 메인 PC 접속이 안돼요
- 메인 PC 방화벽에서 포트 8000 허용 확인
- 메인 PC 상단 "📋 서브 주소 복사" 로 주소 공유 (v1.2.6+)
- 같은 네트워크(LAN) 에 있는지 확인

---

## 📞 문의

이 리포는 배포 전용이며, 기능 요청·버그 리포트는 별도 채널로 전달해 주세요.

---

## 📄 라이선스

내부 배포용 · 상업적 재사용 금지
