# v1.3.26 — 기록 탭 / 직원 권한 과별 표시 (2026-06-12)

## 기록 상위탭 추가

- 예약탭 옆에 `기록` 상위탭을 추가했습니다.
- 기본 하위탭으로 `메뉴얼`, `C-Arm`을 제공합니다.
- 각 하위탭 이름은 연필 버튼으로 수정할 수 있습니다.
- 하위탭별로 차트번호, 성함, 직원을 입력해 기록을 저장할 수 있습니다.
- 하위탭별 과 카테고리를 설정하면 직원 선택 목록에는 해당 과 직원만 표시됩니다.
- 입력 목록 맨 아래에 직원별 저장 건수가 표시됩니다.

## 직원 권한 표시 보강

- 직원 관리의 권한/치료항목 표시와 저장 시, 직원에게 설정된 과에 속한 항목만 보이도록 보강했습니다.
- 정산 권한 판정에서도 다른 과 치료항목이 직원 권한으로 섞이지 않게 차단했습니다.

## 데이터 / 동기화

- `record_tab_settings`, `record_entries` 테이블이 자동 생성됩니다.
- 기록 데이터와 하위탭 설정은 다중 PC 동기화 대상에 포함됩니다.
- 기존 `clinic.db`는 AppData에 그대로 유지됩니다.

## 검증

- 기록 API/UI 회귀 테스트 추가 및 통과
- 직원 과별 권한 필터와 정산 권한 회귀 테스트 통과
- 마이그레이션/PyInstaller discovery 테스트 통과
- `main.js` 문법 검사 통과
