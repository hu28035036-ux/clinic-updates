# v1.3.27 — 기록 요일 입력 / 리뷰이벤트 / 직원 과별 표시 보강 (2026-06-12)

## 기록 탭

- 기록 탭에서 주간 요일 버튼으로 날짜별 입력과 조회가 가능합니다.
- 선택한 날짜 기준으로 차트번호, 성함, 직원을 저장합니다.
- 기본 하위탭 `메뉴얼`, `C-Arm`에 `리뷰이벤트` 하위탭을 추가했습니다.
- 이미 입력한 기록은 `수정` 버튼으로 날짜, 차트번호, 성함, 직원까지 변경할 수 있습니다.

## 데이터 / 마이그레이션

- `record_entries.record_date` 필드가 자동 추가됩니다.
- 기존 기록은 `created_at` 날짜를 기준으로 `record_date`가 자동 보정됩니다.
- 기존 `clinic.db`는 AppData에 그대로 유지됩니다.

## 직원 관리

- 비활성 과 직원이 `미분류`로 넘어가지 않고 원래 과 카드에 남도록 수정했습니다.
- 모든 과에서 해당 과에 연결된 치료항목만 권한 배지로 표시합니다.
- 과에 활성 치료항목이 없으면 `과 활성 치료항목 없음` 으로 표시합니다.

## 검증

- 기록 API/UI 회귀 테스트 통과
- 기록 수정, 리뷰이벤트 하위탭, 요일별 조회 검증 통과
- 직원 과별 표시 검증 통과
- `main.js` 문법 검사 통과
- 마이그레이션/PyInstaller discovery 검증 통과
