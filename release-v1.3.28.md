# v1.3.28 — 매출 설정 다중 PC 동기화 보강 (2026-06-13)

## 매출 설정 다중 PC 동기화

- 매출 탭의 순서, 표시 항목, 헤더명, 합계 수식 설정을 서버 저장 대상으로 변경했습니다.
- 다른 PC에서 매출 설정을 바꾸면 `system_setting` 동기화로 따라오도록 보강했습니다.
- 기존 `localStorage` 저장은 fallback으로 유지하되, 공유 기준은 서버 설정으로 이동했습니다.

## 매출 데이터 동기화 병합

- 매출 기록은 날짜+카테고리 기준으로, 일일 업무보고는 날짜 기준으로 병합해 PC별 row id 차이에도 업데이트가 반영되도록 했습니다.
- 매출 기록과 일일 업무보고 삭제 동기화에 날짜/카테고리 payload가 남도록 수정했습니다.

## 안내

- 기존 `clinic.db`는 AppData에 그대로 유지됩니다.

## 검증

- 매출 UI 설정 저장/조회 회귀 테스트 추가
- 다중 PC 자연키 병합/삭제 회귀 테스트 추가
- `node --check app\static\js\main.js` 통과
- 매출/동기화/마이그레이션/PyInstaller discovery 테스트 267건 통과
