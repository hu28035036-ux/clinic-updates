# v1.3.29 — 휴무일 추가 / 비치료사 휴무 표시 수정 (2026-06-14)

## 휴무일 추가 (직원 1명 · 여러 날짜 동시 등록)

- 휴무일 관리 탭에 `➕ 휴무일 추가` 버튼을 추가했습니다.
- 직원을 선택하고 달력에서 여러 날짜를 한 번에 골라 등록할 수 있습니다.
- 선택한 날짜별로 `종일 / 오전반차 / 오후반차` 와 `연차 / 월차` 를 각각 지정합니다.
- 기존 휴무를 지우지 않고 추가/갱신합니다 (같은 날짜의 다른 직원 휴무는 그대로 유지).

## 비치료사 직원 휴무 표시 수정

- 간호과·원무과 등 도수치료를 하지 않는 직원의 휴무가 휴무 달력·미니달력에 표시되지 않던 문제를 수정했습니다.
- 휴무 등록은 전 직원 대상인데 표시는 치료사만 매칭하던 불일치를 바로잡았습니다.

## 안내

- 기존 `clinic.db` 는 그대로 유지됩니다.

## 검증

- 휴무 추가 API 회귀 테스트 추가 (다중 날짜 등록, 같은 날 다른 직원 휴무 보존, 잘못된 항목 skip)
- `node --check app\static\js\main.js` 통과
- 전체 회귀 테스트 2,241건 통과
- 빌드 exe 격리 환경 스모크 + ZIP 추출 구조 검증 통과
