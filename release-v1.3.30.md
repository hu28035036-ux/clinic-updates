# v1.3.30 — 미니달력 깨짐 / 금일목록 "?" 표시 수정 (2026-06-15)

## 미니달력 깨짐 수정

- 미니달력이 고정 높이(320px)라, 휴무명·환자수 배지가 많은 달에는 내용이 넘쳐
  내부 스크롤바가 생기고 날짜 칸이 잘려 깨진 것처럼 보이던 문제를 수정했습니다.
- 달력 높이를 내용에 맞춰 자동으로 늘어나도록(`height: auto`) 변경했습니다.
- 한 날짜에 휴무자가 많으면 칸이 과도하게 길어지므로 휴무명은 최대 3개 + `+N` 으로
  축약 표시합니다 (전체 명단은 마우스를 올리면 툴팁으로 표시).

## 금일 예약 환자 / 취소 목록에 "?" 로 표시되던 문제 수정

- 예약에서 신규 환자를 막 등록한 직후, 환자 목록 동기화 전이면 금일 예약 환자·취소
  목록과 예약 상세창에 환자 이름이 `?` 로 나오던 문제를 수정했습니다.
- 서버가 예약 정보와 함께 내려주는 환자 이름·차트번호로 대체 표시하도록 했습니다.
  (우측 예약표는 이미 같은 방식이라 정상이었고, 목록·상세창도 동일하게 맞췄습니다.)

## 안내

- 기존 `clinic.db` 는 그대로 유지됩니다.

## 검증

- 신규 환자 등록 직후 캐시 미동기화 상태를 재현 → 수정 후 이름 정상 표시 확인
- 미니달력 내부 스크롤바 제거 및 월 이동 시 내용 넘침 없음 확인
- `node --check app\static\js\main.js` 통과
- 전체 회귀 테스트 2,241건 통과 · ruff 통과
- 빌드 exe 격리 환경 스모크(HTTP 200 · 버전 v1.3.30) + ZIP 구조 검증 통과
