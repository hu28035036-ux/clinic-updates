# v1.3.31 — 관리자 비밀번호 간헐적 "틀렸다" 오류 수정 (2026-06-16)

## 증상

올바른 관리자 비밀번호를 입력해도 가끔 "비밀번호가 올바르지 않습니다"로 거부되는 문제.

## 원인

여러 PC 접속·백그라운드 작업(동기화/백업)이 동시에 설정 파일(`config.json`)을 저장할 때,
공유하던 고정 임시파일을 서로 덮어써 `config.json`이 깨졌습니다.
이후 프로그램이 깨진 설정을 읽으면 기본값으로 재생성하면서 **관리자 비밀번호가 초기값
(`admin1234`)으로 되돌아가**, 사용자가 변경한 비밀번호가 거부되었습니다.

## 수정

- 설정 저장(`save_config`)을 **호출별 고유 임시파일 + 잠금**으로 직렬화해 동시 저장 시
  파일이 깨지지 않도록 했습니다.
- 설정이 어떤 이유로든 손상되더라도, **관리자 비밀번호 해시·노드ID·동기화 비밀값을
  복구해 보존**합니다. 더 이상 `admin1234`로 초기화되지 않습니다.
- Windows에서 다른 작업이 설정 파일을 읽는 중 저장이 실패(접근 거부, WinError 5)하던
  경우를 짧은 재시도로 보완해, 비밀번호 변경이 조용히 사라지지 않도록 했습니다.

## 안내

- 기존 `clinic.db`는 그대로 유지됩니다.
- 이번 업데이트 이전에 비밀번호가 `admin1234`로 되돌아간 적이 있다면, 업데이트 후
  관리자 탭에서 원하는 비밀번호로 다시 변경해 주세요.

## 검증

- 설정 동시 저장/손상 시 비밀번호 보존 회귀 테스트 추가(3건)
- 전체 회귀 테스트 2,244건 통과 · ruff 통과
- 빌드 exe 격리 환경 end-to-end 스모크: 비밀번호 변경 → 설정 손상 → 변경한
  비밀번호 로그인 성공 + `admin1234` 거부 확인 (v1.3.31)
