# v1.3.34 — 장기 데이터 성능 보강 + 환자관리탭 개선 (2026-06-16)

## 배경

예약·기록이 수년간 쌓이면(5년 이상) 일부 화면이 느려질 수 있는 지점들이
있었습니다. 데이터가 많아져도 항상 빠르게 동작하도록 미리 손봤습니다.

## 변경

- **조회 속도 개선 (장기 누적 대비)**
  - 환자 치료이력 / 마지막 예약 조회용 인덱스를 추가했습니다. 예약이 수십만
    건이어도 환자 상세(치료이력) 열기가 즉시 처리됩니다.
  - 치료항목 삭제 시 "참조 예약 확인"이 전체 예약을 메모리에 올리던 방식을
    DB 검색으로 바꿨습니다(수 초 + 수백 MB → 수십 ms).
  - 환자 치료이력 조회의 반복 쿼리(N+1)를 1회 조회로 줄였습니다.
- **자동 유지보수 강화**: 매일 도는 정리 작업에 'AI 사용로그 6개월 보존 정리'와
  'DB 통계 갱신(PRAGMA optimize)'을 추가했습니다 — 데이터가 커져도 조회 속도를
  계속 빠르게 유지합니다.
- **환자관리탭 '마지막 예약'**: 화면에 보이는 환자만 계산하도록 바꿨습니다.
  전체 환자를 매번 통째로 계산하던 방식을 없애, 환자 수가 많아도 속도가 일정합니다.

## 버그 수정

- 환자관리탭 **'최근 검색 환자' 목록에 이미 삭제된 환자가 계속 남아 보이던
  문제**를 수정했습니다. (최근목록은 PC별로 저장되어, 다른 PC나 동기화로 삭제된
  환자가 그 PC에는 남아 있었습니다. 이제 목록을 열 때 서버에서 존재 여부를 확인해
  삭제된 환자를 자동으로 정리합니다.)

## 안내

- 기존 `clinic.db`(환자·예약·매출 등)는 그대로 유지됩니다. 인덱스만 추가됩니다.

## 검증

- 환자관리 last_visit / 최근목록 삭제정리 회귀 테스트 5건 추가
- 인덱스·쿼리플랜·실측(예약 15만 건 격리 DB)으로 속도 개선 확인
- 빌드 exe 격리 환경 스모크: 기동/버전(1.3.34)/마이그레이션 37건(신규 인덱스 포함) 확인
- 브라우저 스모크: 삭제 환자 자동정리 + '마지막 예약' 컬럼 정상 표시 확인
- 전체 회귀 테스트 2,253건 통과 · ruff · `node --check` 통과
