# v1.3.35 — 예약 현황표 좁은 화면 가로 스크롤 + 스크롤 위치 유지 (2026-06-16)

## 배경

창을 좁게 줄여서 다른 프로그램과 나란히 띄워 쓸 때, 예약 현황표의 오른쪽
열(치료사)이 잘리고 옆으로 넘겨볼 방법이 없었습니다. 또 표가 몇 초마다 자동
동기화될 때마다 보던 위치가 맨 왼쪽으로 되돌아가 불편했습니다. 두 가지를
모두 손봤습니다.

## 변경

- **좁은 화면 가로 스크롤**: 창을 좁게 줄였을 때 예약표 영역을 화면 안에 맞추고,
  표 아래에 가로 스크롤바가 생기도록 했습니다. 이제 잘린 치료사 열을 옆으로
  넘겨서 볼 수 있습니다.
- **스크롤 위치 유지**: 예약표가 자동으로 동기화될 때(몇 초 주기) 보던 위치가
  맨 왼쪽으로 되돌아가던 문제를 수정했습니다. 이제 동기화가 일어나도 가로·세로
  스크롤 위치가 그대로 유지됩니다. (동기화 주기는 그대로 두고 위치만 보존)

## 안내

- 기능·데이터 변경 없이 화면 동작만 개선했습니다. 기존 `clinic.db`(환자·예약·
  매출 등)는 그대로 유지됩니다.

## 검증

- 좁은 화면(≤1024px, 860×900) 브라우저에서 가로 스크롤바 노출(17px)·하단이 화면
  안에 위치해 드래그 가능 확인
- 자동 동기화와 동일하게 표를 다시 그려도 가로·세로 스크롤 위치가 보존되는지 확인
- UI 계약 테스트 8건 + 전체 회귀 테스트 통과
