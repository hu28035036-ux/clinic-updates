# v1.3.37 — 집계 자동화 + 기록 탭 치료항목화 (2026-06-18)

## 배경

집계를 매번 직접 숫자로 입력해야 했고, 기록 탭은 메뉴얼 / C-ARM / 리뷰이벤트
3개로 고정돼 있었습니다. 이제 치료완료(예약)와 기록에서 집계가 자동으로
채워지고, 기록 탭도 치료항목에서 자유롭게 구성할 수 있습니다.

## 변경

- **집계 자동화**: 직접 입력하던 방식에서, 치료완료(예약 완료)와 기록에서
  자동으로 집계되도록 바꿨습니다. 예약·기록에 없는 당일 방문(체외충격파
  walk-in 등)만 칸의 숫자를 올려 직접 더하면 됩니다.
  (칸 위에 마우스를 올리면 자동/수동 내역이 보입니다.)
- **기록 탭 치료항목화**: 메뉴얼 / C-ARM / 리뷰이벤트 고정 탭을, 치료항목에서
  "기록 필요"를 켠 항목으로 자유롭게 구성할 수 있게 바꿨습니다.
- **기록 → 집계 자동 반영**: 기록에 입력한 직원별 건수가 집계에 자동으로
  반영됩니다.
- **정산 분리**: 집계를 확인한 뒤 [정산 반영]을 누르면 정산에 반영됩니다.
  집계는 항상 자동, 정산은 버튼을 누를 때만 반영됩니다.

## 기존 데이터 (그대로 보존)

- 기록(메뉴얼 / C-ARM / 리뷰이벤트)에 입력해둔 내용은 같은 이름의 치료항목으로
  자동 이전됩니다. 같은 이름의 치료항목이 이미 있으면 그 항목을 재사용합니다.
- 정산에 반영해둔 내용은 그대로 유지됩니다.

## 버그 수정

- 기록용 치료항목이 도수치료 통계·시간 한도 계산에 잘못 섞이던 문제를
  수정했습니다.

## 업데이트 후 확인

- 도수치료를 치료완료로 집계하신다면, 이름이 겹쳐 새로 만들어진 "메뉴얼"
  기록항목이 필요 없을 경우 [관리 → 치료항목]에서 삭제하세요.
- 리뷰이벤트에 인센티브를 주시려면 [관리 → 치료항목]에서 리뷰이벤트 항목에
  인센티브를 설정하세요.

## 검증

- 치료항목 "기록 필요" → 기록 탭 등장 → 기록 입력 → 집계 자동 반영,
  치료완료(예약) → 집계 자동 반영, 수동 보정 가산을 브라우저(격리 DB)에서 확인
- 업그레이드 마이그레이션을 통제 환경에서 재현 — 기록 4건 자동 이전 +
  같은 이름(C-Arm) 치료항목 재사용(중복 생성 없음) 확인
- 전체 회귀 테스트 2,256건 + ruff + node 문법 검사 통과
