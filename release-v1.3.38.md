# v1.3.38 — 환자 엑셀 변환: 환자번호 인식 + 주민번호로 생년월일 (2026-06-18)

## 배경

환자 데이터를 엑셀로 불러올 때, 일부 환자목록은 "차트번호" 대신 "환자번호"
열을 쓰고, 생년월일 열 없이 주민번호만 들어 있는 경우가 있었습니다. 이 경우
차트번호와 생년월일이 비어버려 등록 후 일일이 손봐야 했습니다.

## 변경

- **환자번호 인식**: 데이터 변환에서 "환자번호" 열을 차트번호로 인식하도록
  했습니다. (기존에는 "차트번호" 열만 인식)
- **주민번호로 생년월일 자동 작성**: 생년월일 열이 없거나 비어 있으면 주민번호
  앞 6자리로 생년월일을 채웁니다.
  - 주민번호 **7번째 숫자**로 1900년대 / 2000년대를 정확히 구분합니다.
    (1·2 → 1900년대, 3·4 → 2000년대, 9·0 → 1800년대)
  - 주민번호에 "-" 가 있어도 없어도 동일하게 처리됩니다.
  - 생년월일 열이 따로 있으면 그 값을 우선 사용합니다.
- **중복 등록 방지**: 이미 등록된 환자와 차트번호(환자번호)가 같거나,
  이름 + 생년월일이 같으면 등록하지 않습니다.
  - 이름이 같아도 생년월일이 다르면 다른 환자로 등록합니다(동명이인 보호).

## 검증

- 실제 환자목록(238명) 변환에서 환자번호 → 차트번호, 주민번호 → 생년월일
  (세기 구분 정확), 성별·휴대폰 정상 인식 확인
- 회귀 테스트 6건 추가, 전체 회귀 2,262건 + ruff 통과
