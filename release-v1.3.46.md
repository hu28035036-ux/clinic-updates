# v1.3.46 — 재고관리 과 선택·엑셀형·검색·저장·관리자 전용 칸 + 예약 메모/저장 통합 + 차트 카드 (2026-06-26)

## 재고관리

- **과(부서)별로 길게 나열하던 재고**를 치료기록처럼 **"과 선택" 방식**으로 바꿨습니다.
  위에서 과를 고르면 그 과의 재고만 보입니다.
- 표를 **엑셀처럼 촘촘한 격자**로 정리하고, 입력 칸·글자 크기를 줄였습니다.
  입력란과 ✎·× 버튼의 테두리·배경을 없애 품목이 많아도 한눈에 보입니다.
- **품목·단위 검색**과 **"저장" 버튼**을 추가했습니다. 여러 칸을 입력한 뒤 한 번에
  저장하며, 바뀐 칸은 노랗게 표시됩니다 (저장 안 하고 과 이동·새로고침 시 경고).
- 관리 열을 추가할 때 **"🔒 관리자 전용 칸"**으로 지정할 수 있습니다.
  - 일반 재고 칸은 로그인 없이 **직원도 입력**, 관리자 전용 칸은 **관리자만 입력**합니다.
  - 품목·관리 열 추가/수정/삭제 등 구성 변경은 관리자만 가능합니다 (직원 화면엔 셋업
    버튼·관리 열이 보이지 않습니다).

## 예약 / 차트기록

- 예약을 잡은 뒤에도 예약 상세창에서 **"당일메모"를 바로 수정**할 수 있습니다.
- 예약 상세창 저장 버튼 5개(메모·신환·시간·치료항목·담당 배정)를 **"💾 저장" 하나로
  통합**했습니다 (치료완료·예약 취소·완전 삭제는 그대로 별도 버튼).
- 차트기록 작성 화면 "스페셜테스트"·"ROM/MMT" 카드를 **왼쪽 → 오른쪽으로 이동**했습니다
  (작성 칸이 왼쪽).

## 검증

- 전체 회귀 테스트 2,309건 통과 + 코드 문법·린트 검사 통과.
- 격리 환경 브라우저 E2E로 재고(과 선택·엑셀형·검색·저장·관리자 전용 칸 권한)·예약 메모·
  차트 카드를 직접 확인. 빌드 exe 스모크(버전 1.3.46·DB·마이그레이션 42)도 확인했습니다.
- 재고 관리 열에 "관리자 전용" 컬럼이 추가됩니다(마이그레이션). 기존 재고 데이터는 보존됩니다.
