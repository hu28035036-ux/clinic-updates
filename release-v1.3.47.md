# v1.3.47 — 재고 관리자 전용 칸: 로그인 안 하면 칸 자체를 숨김 (2026-06-26)

## 재고 관리자 전용 칸

- 재고의 **"관리자 전용 칸"**을 관리자 로그인이 안 된 상태(직원)에서는 **칸(열) 자체가
  보이지 않도록** 했습니다.
  - 바로 앞 버전(v1.3.46)에서는 값이 보이고 수정만 막혀 있었는데, 이제 **값 자체를
    확인할 수 없습니다.**
  - 서버 응답에서도 관리자 전용 칸과 값을 빼므로, 화면뿐 아니라 어떤 방법으로도 직원은
    그 칸을 볼 수 없습니다.
  - 관리자로 로그인하면 칸과 값이 나타나고, **로그아웃하면 즉시 다시 가려집니다.**

## 검증

- 전체 회귀 테스트 2,310건 통과 + 코드 문법·린트 검사 통과.
- 격리 환경 브라우저 E2E로 미인증 응답에서 전용 칸/값 제외, 직원 화면 칸 숨김, 관리자
  저장·로그아웃 즉시 가림, 일반 칸 직원 저장을 직접 확인. 빌드된 프로그램에서도 권한
  숨김 동작을 확인했습니다.
- 백엔드·화면만 변경, DB/마이그레이션 변동 없음(관리자 전용 칸 컬럼은 v1.3.46에서 추가됨).
