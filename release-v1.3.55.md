# v1.3.55 — 휴무 카운트·과별 보기 + 집계→정산 자동 반영 (2026-07-16)

## 휴무일 관리가 당직처럼 바뀝니다

- **"과별 휴무 일수" 요약**이 달력 위에 생깁니다 (당직의 "과별 당직 횟수"와 같은 방식).
  - 보고 있는 달 기준으로, 직원별 휴무 일수를 과별로 묶어 보여줍니다.
  - 휴무가 있는 직원만 많은 순으로 나오고, 달을 넘기면 그 달 기준으로 바뀝니다.
- **"과" 선택 필터**가 생깁니다 (당직과 동일한 여러 개 동시 선택 방식).
  - 선택한 과의 직원 휴무만 달력·요약에 표시됩니다. 최소 1개 과는 유지됩니다.
  - 처음 열면 전체 과가 선택된 상태라, **기존처럼 전 직원 휴무가 다 보입니다**.

## 정산에 자동으로 반영됩니다 ("정산 반영" 버튼 제거)

- 이제 집계 탭에서 **"정산 반영"을 누를 필요가 없습니다** (버튼이 없어집니다).
- **정산 탭에서 기간을 조회하면** 그 기간 집계가 자동으로 반영되어 나옵니다.
- 집계 칸(walk-in 포함)을 고친 내용도 정산을 조회하면 바로 반영됩니다.

> ⚠️ **참고**: 정산 금액은 **조회하는 시점의 수가·인센티브**로 계산됩니다.
> 따라서 치료항목 수가를 바꾸면 **과거 기간 정산 금액도 새 수가로 다시 계산**됩니다.
> (정산이 항상 최신 집계·수가 기준으로 보이도록 한 것입니다.)

## 집계 칸이 작아집니다

- 집계 표의 카운팅 칸을 가로·세로로 줄여 한 화면에 더 많이 보입니다.
  (칸 60×34 → 46×26, 입력칸도 함께 축소)

## 검증

- 전체 회귀 테스트 2,318건 통과 + 린트·문법 검사 통과.
- 격리 환경 브라우저 점검: 휴무 요약·과 필터 동작(최소 1개 유지 포함),
  집계→정산 자동 반영, 칸 크기 확인.
- DB·마이그레이션 변동 없음 (정산 반영 API 에 내부 옵션 1개만 추가).

> 업데이트 후 화면이 그대로 보이면 **Ctrl+Shift+R** 로 새로고침하세요.
