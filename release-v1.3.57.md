# v1.3.57 — 기록 탭 메모 + 야간당직 퇴근시간·시간 집계 (2026-07-24)

> ※ 당초 v1.3.56 으로 준비되었으나, 정산 확정(잠금) 릴리스가 v1.3.56 으로 먼저
> 배포되어 이 기능은 v1.3.57 로 번호를 재배정했습니다. 내용은 동일합니다.

## 기록 탭에 "메모" 칸이 생깁니다

- 기록 탭의 **차트번호·성함·직원 옆에 "메모" 칸**이 추가됐습니다 (선택 입력, 최대 200자).
  - 추가 폼과 목록 표, 수정 창에서 메모를 입력·확인할 수 있습니다.
  - 메모는 선택 사항 — **차트번호 또는 성함은 여전히 필수**이며, 메모만 단독으로는 저장되지 않습니다.

## 야간당직에 "퇴근시각"을 입력하고 시간을 집계합니다

- **야간당직에 직원별 퇴근시각을 날짜마다 입력**할 수 있습니다.
  - 날짜 클릭 설정 창에서 당직 직원을 체크하면 옆에 퇴근시각 칸이 나옵니다
    (같은 날 여러 명이면 각자 다른 시각 입력 가능).
  - "야간당직 추가"(직원 1명·여러 날짜) 창에서는 선택한 모든 날짜에 **공통 퇴근시각**을 한 번에 넣습니다.
- **기준 퇴근시간 이후 근무분을 "야간당직 시간"으로 집계**합니다.
  - "과별 당직 횟수" 요약에 직원별 시간이 함께 표시됩니다 (예: 홍길동 5회 · 12시간 30분).
  - 기준 퇴근시간은 **관리자 → 시스템 → 운영 시간의 "야간당직 기준"**에서 설정합니다
    (기본 18:30, 운영 종료시간과 별개). 자정을 넘겨 퇴근한 경우도 바르게 계산됩니다.
- 아침당직·점심당직에는 퇴근시간/시간 집계가 적용되지 않습니다 (야간당직 전용).

## 검증

- 기록 메모 왕복 테스트 + 당직 테스트(퇴근시각 왕복·초과분 계산·마이그레이션) + 린트 통과.
- 격리 환경 런타임 점검: 메모 생성/수정/목록 반영, 야간당직 초과시간 계산(90분),
  아침당직 초과 0, 점심당직 공존, 잘못된 시각 거부.
- DB 마이그레이션 m044(기록 메모) + m045(야간당직 퇴근시각) — 둘 다 컬럼 추가만,
  기존 데이터 전량 보존·재실행 안전.

> 업데이트 후 화면이 그대로 보이면 **Ctrl+Shift+R** 로 새로고침하세요.
