# v1.3.58 — 자동 업데이트 준비 (화면·기능 변화 없음) (2026-07-25)

> **화면과 기능은 v1.3.57 과 완전히 같습니다.** 새 기능도, 바뀐 버튼도 없습니다.
> 그런데도 설치해야 하는 이유가 있습니다 — 바로 아래에 적었습니다.

## 왜 설치해야 하나요

다음 대형 업데이트에서 **프로그램 이름이 바뀝니다.**

업데이트를 실제로 수행하는 것은 새로 받은 버전이 아니라, **이미 이 PC 에 깔려 있는 업데이터**입니다.
그런데 지금까지의 업데이터는 새로 받은 압축 파일 안에서 `도수치료예약.exe` 라는 **이름을 글자 그대로**
찾도록 만들어져 있었습니다. 이름이 바뀐 버전이 오면 그 파일을 찾지 못해 **업데이트를 포기하고 이전
버전으로 되돌립니다.** 다음에 다시 확인해도 똑같이 실패합니다.

그래서 **이름에 유연한 업데이터를 먼저 깔아 두는 것**이 이 버전의 목적입니다.
이 버전을 설치해 두면 다음 대형 업데이트가 자동으로 넘어갑니다.
설치하지 않으면 다음 업데이트를 자동으로 받을 수 없고, 프로그램을 직접 내려받아
수동으로 교체해야 합니다.

## 무엇이 바뀌었나 (전부 내부 동작)

- 본체가 **현재 실행 파일 이름을 업데이터에 알려줍니다.** 업데이터는 그 이름으로
  "무엇을 종료하고 무엇을 백업할지"를 정합니다.
  - 알려주지 못한 경우에도 폴더에 있는 `*.exe` 를 직접 찾아 동작합니다.
- 업데이터가 **새 압축 파일 안의 실행 파일을 이름과 상관없이 찾습니다**
  (`_internal` 폴더 안의 파일은 제외 — 프로그램 본체가 아니기 때문입니다).
  - 압축 구조가 `폴더\프로그램.exe` 든 `프로그램.exe` 든 둘 다 처리합니다.
- **이름이 바뀌는 업데이트에서는 바탕화면 바로가기를 자동으로 다시 만듭니다.**
  옛 이름을 가리키던 바로가기는 정리하고 새 이름으로 만듭니다.
  - 이번 업데이트는 이름이 그대로이므로 **바로가기에 아무 변화가 없어야 정상입니다.**
- 업데이터 파일(`updater.bat`)의 줄바꿈이 잘못 저장돼 있던 문제를 바로잡았습니다.
  Windows 명령 처리기가 스크립트를 잘못 읽어 **업데이트가 이유 없이 되돌려질 수 있는**
  상태였습니다 (회귀 테스트로 고정).
- 업데이터 로그 파일 이름을 `clinic_updater.log` 로 통일했습니다 (프로그램 이름과 분리).

## 데이터

- **환자·예약·정산·기록 데이터는 전혀 건드리지 않습니다.** DB 구조 변경도, 마이그레이션도 없습니다.
- 업데이트 직전 자동 백업은 이전과 똑같이 수행됩니다.

## 검증

- 업데이터 통합 테스트 **신규 7건** — 실제로 업데이터를 실행해 파일 교체 결과를 확인했습니다.
  이름이 바뀌는 교체 / 이름이 같은 교체 / 이름을 못 받았을 때의 폴더 탐색 /
  압축 구조 두 가지 / 압축 파일이 없을 때 이전 버전으로 되돌리기 / 줄바꿈 회귀 가드.
- 업데이터에 이름을 전달하는지 확인하는 테스트 3건.
- 전체 회귀 테스트 **2,342건 통과** + 린트 통과.
- 바탕화면 바로가기는 자동 테스트가 닿지 않는 구간이라 **격리된 임시 폴더에서 수동으로
  실행해 확인**했습니다 (이름이 바뀌는 시나리오 — 새 바로가기 생성, 옛 바로가기 정리,
  관계없는 기존 바로가기 17개는 그대로).
- 클라우드 Windows 빌드로 배포 ZIP 을 미리 만들어, 안에 들어간 업데이터가 검증한 파일과
  **바이트 단위로 동일**한지 확인했습니다.

> 이 버전은 화면 변화가 없으므로 업데이트 후 화면이 그대로 보이는 것이 정상입니다.
> (혹시 이상해 보이면 **Ctrl+Shift+R** 로 새로고침하세요.)
