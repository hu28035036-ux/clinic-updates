# 병원관리 v2.0.0

사내 채팅 추가 · 프로그램 이름 변경 (도수치료예약 → 병원관리) · AI 기능 제외 · 안정성 개선

> ⚠ **v1.3.58 을 먼저 설치해 주세요.** 이번 업데이트는 프로그램 이름이 바뀝니다.
> 업데이트를 수행하는 것은 이미 PC 에 깔려 있는 업데이터인데, v1.3.57 이하의
> 업데이터는 옛 이름을 글자 그대로 찾기 때문에 이름이 바뀐 버전이 오면
> 업데이트를 포기하고 되돌립니다. v1.3.58 이 그 문제를 해결한 버전입니다.

## 이름과 데이터

- 실행 파일이 `병원관리.exe` 로 바뀝니다. 바탕화면 바로가기는 자동으로 다시 만듭니다.
- 데이터 폴더가 `%APPDATA%\병원관리\` 로 바뀝니다. 첫 실행 때 자동으로 옮깁니다.
- **옛 폴더는 지우지 않습니다.** 이전에 실패하면 옛 폴더를 계속 사용하므로
  데이터가 사라지지 않습니다.
- 기존 백업은 용량 때문에 옮기지 않고 옛 폴더에 둡니다. 복원 화면에서는
  옛 백업과 새 백업이 함께 보입니다.

## 사내 채팅 (신규)

- **자리 단위**입니다 — 원장실 · 치료실 · 접수데스크. 사람 계정이 아니라 장소이고
  비밀번호가 없습니다. 교대 근무로 사람이 바뀌어도 자리는 그대로입니다.
- 전체방 + 1:1 + 단체방. 화면이 넓으면 좌우 2단으로 보입니다.
- 전달 표시 ○ → ✓. **서버가 받은 뒤에만 ✓ 가 뜹니다** — 보냈다고 착각하지 않게.
- 대화는 `chat.db` 에 따로 저장됩니다. 채팅이 깨져도 예약 기능은 정상입니다.
- 사진·파일 전송 없음 (환자 개인정보 축적 방지).

## 안정성 (이번 버전에서 새로 넣은 것)

| | 전 | 후 |
|---|---|---|
| 예약 조회 SQL | 요청당 129회 | **4회** |
| 예약표 렌더 | 144 ms | **17.5 ms** |
| 오래 켜뒀을 때 힙 | 계속 증가 | **평평** |
| PC 8대 응답 | 1,225 ms | **376 ms** |

- **복구 모드** — 진료 데이터를 못 열면 프로그램이 아무 말 없이 죽는 대신
  복구 화면이 뜹니다. 원인을 보여주고 백업에서 되돌릴 수 있습니다.
- **포트 주인 확인** — 다른 프로그램이 8000 번을 쓰고 있으면 알려줍니다.
- **기동 오류 기록** — 시작하지 못하면 `%TEMP%\병원관리_기동오류.txt` 에 원인이 남습니다.
- **폴링 백오프** — 서버가 죽으면 재시도 간격을 늘려 복구를 방해하지 않습니다.

## 제거

AI 기능(AI 예약 도우미 · AI 휴무 등록 · 매뉴얼 Q&A · 관리자 AI 설정)이 없어졌습니다.
예약 · 환자 · 정산 · 기록 · 차팅 · 재고 · 문자 기능은 전부 그대로입니다.

## 검증

pytest 1,567 passed / 1 skipped / 10 xfailed · ruff 클린 · DB 경로 안전검사 통과.
