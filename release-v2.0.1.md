# 병원관리 v2.0.1

배포본 정리. **화면·기능·데이터는 v2.0.0 과 동일합니다.**

> v2.0.0 을 이미 설치하셨다면 급하지 않습니다.
> 아직이시면 이 버전으로 바로 오시면 됩니다 — v2.0.0 내용이 전부 들어 있습니다.

## 바뀐 것

- **`도구/build_knowledge_index.py` 제거** — v2.0.0 에서 제외한 AI 기능 시절의
  스크립트가 배포본에 남아 있었습니다. 제거된 `knowledge/` 와 `app/services/rag/` 를
  참조하는 죽은 코드라 실행되지 않고 동작에도 영향이 없었지만, AI 를 넣지 않았다고
  한 버전에 AI 시절 파일이 들어 있는 것은 맞지 않아 정리했습니다.

- **잔재 검사에 `tools/` 추가** — 기존 검사가 `app/`·템플릿·`static/` 만 보고
  `tools/` 를 놓쳤습니다. `dosu_clinic.spec` 의 `datas` 에는 없지만 배포 워크플로의
  ZIP 패키징 단계가 `도구/` 로 복사하기 때문에, **검사 범위와 배포 범위가 어긋나
  있었습니다.** 같은 누락이 다시 생기면 이제 테스트가 막습니다.

## 검증

```
pytest 1,570 passed / 1 skipped / 10 xfailed
ruff   All checks passed
```

가드는 TDD 로 넣었습니다 — 검사를 먼저 추가해 실제 잔재가 잡히는 것(RED)을 확인한 뒤
파일을 지웠고, 검사기 자체가 동작하는지 보는 역가드도 함께 넣었습니다.
