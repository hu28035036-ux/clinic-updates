## v2.0.4 — 업데이트가 설치되지 않던 문제 (브릿지 릴리스)

### ⚠ 이 버전은 실행 파일 이름이 `도수치료예약.exe` 입니다

내용은 최신(v2.0.3 + 수정 3건)이고 **이름만 옛것**입니다. 의도된 것입니다.

설치된 PC 의 업데이트 설치 담당 파일(`updater.bat`)이 구형으로 굳어 있고, 구형은 ZIP 안에서 `도수치료예약.exe` 를 하드코딩으로 찾습니다. v2.0.x 는 `병원관리.exe` 라 매번 `unexpected zip structure` 로 롤백됐습니다. 이 릴리스는 **옛 이름 + 신형 updater 를 함께 실어** 구형도 처리할 수 있게 한 것이고, 설치되는 순간 updater 가 신형으로 교체됩니다. 다음 정식 릴리스가 이름을 자동으로 바로잡습니다.

환자·예약 데이터, 화면에 뜨는 이름, 기능은 모두 정상입니다 (`APP_NAME` 은 config.py 의 별도 상수라 데이터 폴더 이전이 정상 동작합니다).

### 수정

- **배포 ZIP 루트에 `updater.bat` 복구.** spec 후처리가 `dist/도수치료예약/` 를 보고 있었는데 COLLECT name 은 `병원관리` 였고, `if os.path.exists()` 로 감싸여 **조용히 통과**했습니다. v2.0.0 부터 루트에서 빠져 있었습니다. 이제 빠지면 빌드가 멈춥니다.
- **`apply_update` 가 루트 `updater.bat` 을 매번 갱신.** 전에는 `if not exists` 라 한 번 생기면 영구히 안 바뀌었습니다.
- **updater 기동을 확인한 뒤에만 본체 종료.** 확인 안 되면 종료하지 않고 사람이 읽을 수 있는 이유를 돌려줍니다. 전에는 무조건 3초 뒤 `os._exit(0)` 라, updater 가 죽어도 본체가 꺼져 "그냥 꺼졌다" 로만 보였습니다.

### 검증

빈 PC 에 구버전을 설치하고 실제 updater 를 실행했습니다.

| 시나리오 | 결과 |
|---|---|
| v1.3.57(5단계 구형) → v2.0.4 | 성공 · 버전 2.0.4 · updater 6단계로 교체 · **1회로 끝** |
| v2.0.3(6단계) → v2.0.4 | 성공 · 역개명 정상 처리 |
| 대조군: v1.3.57 → v2.0.3 직접 | `[4/5] unexpected zip structure` → rollback (증상 재현) |

pytest 1,681 passed / 2 skipped / 10 xfailed · ruff 통과
